<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mhendrif.mytourismapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="MyApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyTourismApp">
        <activity
            android:name="com.mhendrif.mytourismapp.MainActivity"
            android:theme="@style/Theme.MyTourismApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.mhendrif.mytourismapp.detail.DetailTourismActivity"
            android:label="@string/title_activity_detail_home"
            android:theme="@style/Theme.MyTourismApp.NoActionBar" />
    </application>

</manifest>